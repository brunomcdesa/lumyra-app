import { Text, View } from 'react-native';

export default function ClientsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary">Clientes — em breve</Text>
    </View>
  );
}
