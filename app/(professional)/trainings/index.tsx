import { Text, View } from 'react-native';

export default function TrainingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary">Treinos — em breve</Text>
    </View>
  );
}
