import { Text, View } from 'react-native';

export default function EvaluationsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary">Avaliações — em breve</Text>
    </View>
  );
}
