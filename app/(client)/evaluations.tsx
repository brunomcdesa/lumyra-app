import { Text, View } from 'react-native';

export default function ClientEvaluationsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary">Minhas avaliações — em breve</Text>
    </View>
  );
}
