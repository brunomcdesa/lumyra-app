import { Text, View } from 'react-native';

export default function ClientTrainingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary">Meus treinos — em breve</Text>
    </View>
  );
}
