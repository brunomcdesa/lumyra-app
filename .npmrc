# Deps web transitivas (react-dom 19.2.x via expo-router) pedem react mais novo
# que o pinado pelo react-native (react 19.2.3). O app e mobile e nao usa react-dom;
# legacy-peer-deps mantem o install consistente em qualquer contexto/CI.
legacy-peer-deps=true
